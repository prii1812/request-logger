pipeline {
  agent any

  stages {
    
    stage('Build Image') {
      steps {
      sh "docker build -t prii1812/request-logger:${env.BUILD_ID} -f Dockerfile-multi ."
      sh "docker tag prii1812/request-logger:${env.BUILD_ID} prii1812/request-logger:latest"
      }
    }
    
  }

  post {
    success {
      withCredentials([usernamePassword(credentialsId: 'docker-hub-credentials', usernameVariable: 'USERNAME', passwordVariable: 'PASSWORD')]) {
        sh "docker login -u ${USERNAME} -p ${PASSWORD}"
        sh "docker push prii1812/request-logger:${env.BUILD_ID}"
        sh "docker push prii1812/request-logger:latest"
      }
    }
  }
}
